apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.resultApp.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.resultApp.service.type }}
  selector:
    app: {{ .Values.resultApp.name }}
  ports:
    - port: {{ .Values.resultApp.service.port }}
      targetPort: {{ .Values.resultApp.service.port }}
